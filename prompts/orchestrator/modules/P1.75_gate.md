# Phase 1.75: Complete Gate

## 目的
次フェーズに進む前に、全ての必須条件を満たしていることを確認する。

## チェックリスト（全て満たすまで進まない）
1. [ ] `docs/inbox/` は `.gitkeep` のみ
2. [ ] `docs/tasks/`: DONE には Report パスと DoD チェック記録が存在
3. [ ] `docs/HANDOVER.md`: Latest Orchestrator Report が最新を指す
4. [ ] `node scripts/todo-sync.js` を実行済み
5. [ ] `git status -sb` がクリーン（Gitリポジトリではない場合はスキップ可能）
6. [ ] `node .shared-workflows/scripts/report-validator.js --profile handover docs/HANDOVER.md REPORT_CONFIG.yml .` を実行
7. [ ] 直近のレポートに対して `report-validator.js` を実行
8. [ ] **テスト検証**: DONE タスクのレポートにテスト実行結果が記録されているか確認（テスト不要の場合は理由が明記されているか）
9. [ ] **マイルストーン整合**: `docs/MILESTONE_PLAN.md` の進捗が実際のタスク状況と一致しているか確認

## 未達時の対応
- 未達の項目があれば P1 または P1.5 に戻る
- チャット上で「未完了」と明言し、残タスクを提示

## 完了条件
- 全チェック項目が [x]

## 次フェーズ
P2（状況把握）

